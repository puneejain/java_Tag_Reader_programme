<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.toll</groupId>
    <artifactId>fastag-app</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>fastag-app</name>
    <description>Demo project for Spring Boot RFID Reader</description>

    <dependencies>
        <!-- Spring Boot Web Starter (for REST APIs, controllers) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Data JPA (for database access) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Thymeleaf (optional, only if using server-side templates) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- MySQL Driver -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- WebSocket support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
    <!--    
        <dependency>
    <groupId>com.rfid</groupId>
    <artifactId>reader-sdk</artifactId>
    <version>1.0</version>
    <scope>system</scope>
    <systemPath>C:\Users\asus\Downloads\project backup\fastag-app/lib/ReaderAPI.jar</systemPath>
</dependency>-->

<dependency>
    <groupId>com.rfid</groupId>
    <artifactId>readerapi</artifactId>
    <version>1.0</version>
</dependency>
<dependency>
        <groupId>com.jacobid</groupId>
        <artifactId>jacob</artifactId>
        <version>1.0</version>
    </dependency>

    <!-- RXTX Jars -->
    <dependency>
        <groupId>com.rxtx</groupId>
        <artifactId>rxtxcomm</artifactId>
        <version>1.0</version>
    </dependency>
    <dependency>
        <groupId>com.rxtx</groupId>
        <artifactId>rxtxcomm21</artifactId>
        <version>1.0</version>
    </dependency>
    <dependency>
        <groupId>com.rxtx</groupId>
        <artifactId>rxtxcommMFZ</artifactId>
        <version>1.0</version>
    </dependency>
    <dependency>
        <groupId>com.rxtx</groupId>
        <artifactId>rxtxcommNew</artifactId>
        <version>1.0</version>
    </dependency>

    <!-- SCITA -->
    <dependency>
        <groupId>com.scita</groupId>
        <artifactId>avcc</artifactId>
        <version>1.0</version>
    </dependency>

    <!-- Symbol RFID -->
    <dependency>
        <groupId>com.symbol</groupId>
        <artifactId>rfid-api3</artifactId>
        <version>1.0</version>
    </dependency>
        <!-- Lombok (helps reduce boilerplate code) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.32</version> <!-- latest stable -->
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>

        <!-- For testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Spring Boot Maven Plugin -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
